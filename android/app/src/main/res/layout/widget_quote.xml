<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:background="@drawable/widget_background"
    android:gravity="center_vertical">

    <!-- Barre verticale décorative orange -->
    <TextView
        android:layout_width="3dp"
        android:layout_height="36dp"
        android:background="#C8956C"
        android:text=""
        android:layout_marginEnd="12dp" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- Titre : petit, espacé, uppercase — sobre et élégant -->
        <TextView
            android:id="@+id/widget_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="MESSAGE DU JOUR"
            android:textColor="#C8956C"
            android:textSize="7.5sp"
            android:textStyle="normal"
            android:textAllCaps="true"
            android:letterSpacing="0.18"
            android:fontFamily="sans-serif"
            android:layout_marginBottom="5dp" />

        <!-- Citation : italique + medium weight = élégance garantie -->
        <TextView
            android:id="@+id/widget_quote"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Vous meritez tout le bonheur du monde."
            android:textColor="#3D2B1F"
            android:textSize="15sp"
            android:textStyle="italic"
            android:lineSpacingMultiplier="1.4"
            android:maxLines="2"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium" />

    </LinearLayout>

</LinearLayout>